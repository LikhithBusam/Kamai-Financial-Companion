# AutoGen
autogen-agentchat
autogen-ext[openai,mcp]

# HTTP client for Gemini/Groq (OpenAI-compatible REST endpoints) and the
# Supabase REST calls in autogen_runtime.py
requests

# Configuration
pyyaml

# Rich terminal UI
rich

# Async support
anyio

# Optional: Web framework if we need HTTP endpoints
fastapi
uvicorn[standard]
python-multipart

# Auth: verifying Supabase-issued JWTs
PyJWT

# Database (for direct queries if needed)
psycopg2-binary
sqlalchemy
